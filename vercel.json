{
  "version": 2,
  "bunVersion": "1.3.9",
  "installCommand": "bun install --allow-scripts",
  "buildCommand": "bunx prisma generate && bun run build",
  "framework": "nestjs",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js"
    }
  ]
}